#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <time.h>

int main() {
    int fd;
    char buffer[100];

    /* Check file existence */
    [cite_start]if (access("demo.txt", F_OK) == 0) [cite: 61, 62]
        printf("File exists\n");
    else
        printf("File does not exist\n");

    /* Check read permission */
    [cite_start]if (access("demo.txt", R_OK) == 0) [cite: 63]
        printf("Read permission available\n");
    else
        printf("Read permission NOT available\n");

    /* Open or create file */
    [cite_start]fd = open("demo.txt", O_CREAT | O_RDWR, 0644); [cite: 64]

    /* Write to file */
    [cite_start]write(fd, "Hello UNIX System Calls\n", 24); [cite: 65]
    
    /* Move file pointer to beginning */
    [cite_start]lseek(fd, 0, SEEK_SET); [cite: 65]

    /* Read from file */
    [cite_start]read(fd, buffer, 24); [cite: 66]
    buffer[24] = '\0'; 
    [cite_start]printf("Data read: %s", buffer); [cite: 66]

    /* Get file access time */
    struct stat info;
    [cite_start]stat("demo.txt", &info); [cite: 67]
    [cite_start]printf("\nLast access time: %s", ctime(&info.st_atime)); [cite: 67]
    
    [cite_start]close(fd); [cite: 67]
    return 0;
}